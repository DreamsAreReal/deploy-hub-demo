on:
  push: {branches: [main]}
permissions: {contents: read, packages: write}
jobs:
  deploy:
    uses: DreamsAreReal/deploy-hub/.github/workflows/deploy.yml@main
    with: {app: deploy-hub-demo}
    secrets: inherit
